---
날짜: 2026-08-22
제목: 지표 엔진 버그 수정·10개 지표 완성, origin/main 병합, /progress 커맨드 작성
---

# 오늘 한 일

- **로컬 보안 설정 정리**: `.gitignore`에 `.claude/settings.local.json`, `.mcp.json`을 추가해 개인 로컬 권한/자격증명 정보가 커밋 대상에서 빠지도록 함. (병합 과정에서 `.mcp.json`이 과거에 이미 `origin/main`에 커밋돼 있었던 것을 발견해 `git rm --cached`로 추적만 해제함 — 과거 커밋 이력 자체는 남아 있음)
- **목표 잔류율(retention_target_rate) 출처 미확인 문제 문서화**: `app.py`의 `TARGET_RETENTION_RATE` 상수 위에 출처 미확인 TODO 주석 추가, 잔류율 게이지 옆에 "⚠️ 목표값(90%)은 출처 미확인 가정값입니다" caption 추가. `지표정의서.md` 7번 항목에 "왜 이 지표를 엔진(calc_metrics.py)에 편입하지 않고 app.py에서 직접 계산하기로 했는지"와 조치 내역을 기록.
- **calc_metrics.py 경로·인코딩 버그 수정**:
  - `METRICS_DIR`/`DATA_DIR`/`PATHS`가 클라우드 전용 절대경로(`/mnt/user-data/...`, `/home/claude/...`)로 하드코딩돼 있어 로컬 실행이 무조건 실패하던 것을, 실제 폴더 구조(스크립트가 프로젝트 루트, `06_metrics/`·`data/`도 루트 기준 상대경로)에 맞게 수정.
  - docstring의 지원 formula_type 목록에 빠져 있던 `fifo_gap` 추가.
  - Windows 콘솔(cp949)이 em dash(—) 등 일부 문자를 인코딩하지 못해 `--month` 실행 시 `UnicodeEncodeError`로 죽던 문제를 stdout/stderr UTF-8 강제 설정으로 수정.
- **06_metrics/ 폴더 복원**: `avg_overtime.md`, `high_overtime_employee_rate.md`, `overtime_increase_rate_3m.md`, `vacancy_gap_days.md`, README.md(fifo_gap 설명 추가)를 06_metrics/로 이동하고 루트의 중복 사본 삭제.
- **프로젝트 루트 README.md 신규 작성**: 기존 루트 README.md는 사실 06_metrics용 내용이 잘못 들어가 있던 것이어서, 프로젝트 전체를 안내하는 내용으로 재작성.
- **hr_01_dashboard 전용 `/progress` 커맨드 작성**: `.claude/commands/progress.md` — 10단계 워크플로우(Define~Approve&Send) 기준으로 산출물 존재 여부를 구조적으로 점검하고, 미완료 항목을 즉시 착수 가능한 작업 목록으로 정리해주는 커맨드.
- **origin/main과 병합(충돌 해결 포함)**: 로컬 8개 커밋과 원격의 26개 커밋(주로 GitHub 웹 업로드로 `app.py`/`HR_직원.csv`/리포트를 직접 수정한 것)이 분기돼 있던 것을 병합. `app.py`·`report/퇴사원인_진단_리포트.md`에서 충돌 발생 — 병합 전 양쪽 diff를 직접 비교해 실제로는 같은 최종 상태에 오늘 추가한 5줄(TODO 주석+caption)만 없는 것/GitHub 첨부파일 링크 잔재 1줄만 다른 것임을 확인 후 해결. `.claude/settings.json`도 원격에 이미 트래킹되어 있던 것을 발견.
- **나머지 6개 지표 정의 파일 추가 및 검증**: `overall_leave_rate`, `dept_leave_rate`, `tenure_years`, `eval_score`, `channel_leave_rate`, `dept_avg_overtime` — 작성 전 `data/HR_직원.csv` 등 실제 컬럼명·값(재직상태/부서/채용경로/평가등급)을 직접 확인해 프론트매터에 반영. `calc_metrics.py --index`(10건 전부 로드) / `--month`(값 검증) 실행으로 확인 완료 — 이제 `06_metrics/`에 지표 10개 전부 존재.
- **루트 잔여 파일 정리**: stale `_INDEX.md`(06_metrics/_INDEX.md로 대체됨) 삭제, 워크플로우 기록·인사이트 초안·조인 규칙 문서 6건을 `docs:` 커밋으로 정리.
- **동작 검증**: `python calc_metrics.py --index`/`--month`, `python -m py_compile app.py`, `streamlit run app.py`(HTTP 200, 콘솔 에러 없음) 모두 재확인.

## 미해결 / 확인 필요

- `01_questions/index.md`에 "Q01 | 직원만족도-고객경험" 항목이 있으나 대응하는 파일이 `01_questions/`·`03_analysis/` 어디에도 없음 — 인덱스 정리 또는 파일 신규 작성 필요.
- `tests/` 폴더(또는 유사 자동 검증 스크립트)가 프로젝트에 전혀 없음.
- Streamlit Cloud에 실제로 배포돼 있는지 저장소·GitHub 메타데이터 어디서도 확인 불가 — 배포 URL이 있다면 별도로 공유 필요.
- `retention_target_rate`(목표값 90%)의 실제 출처는 여전히 미확인 상태(캡션으로 노출만 해둔 상태).

## 관련 산출물

- [calc_metrics.py](../calc_metrics.py)
- [06_metrics/](../06_metrics/) (지표 정의 10건 + `_INDEX.md`)
- [지표정의서.md](../지표정의서.md)
- [app.py](../app.py)
- [README.md](../README.md)
- [.claude/commands/progress.md](../.claude/commands/progress.md)
- [.gitignore](../.gitignore)
