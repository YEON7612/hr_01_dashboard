# 데이터 조인 규칙서 — 팬아웃(Fan-out) 방지

작성일: 2026-08-01
근거: 각 파일의 실제 키 유니크 여부를 직접 검증(아래 표)한 결과를 기반으로 작성. 추측 없음.

**팬아웃이란**: 조인 키가 한쪽 테이블에서 중복될 때, 조인 결과 행 수가 원래보다 부풀어 오르는 현상. 예를 들어 "N:M" 관계인 두 테이블을 그냥 조인하면 행 수가 곱으로 늘어나고, 이 상태에서 합계·평균을 내면 값이 실제보다 몇 배로 부풀려짐 — 조용히 틀리기 때문에 가장 위험한 유형의 버그.

---

## 1. 테이블별 그레인 (실측 결과, 2026-08-01)

| 테이블 | 그레인(한 행 = ?) | 키 유니크 여부 | 행수 |
|---|---|---|---|
| `HR_직원.csv` | 사번 1명 | ✅ `사번` 유니크 (300행=300명) | 300 |
| `HR_퇴사이력.csv` | 퇴사 사건 1건 | ✅ `사번` 유니크 (재입사·재퇴사 케이스 없음, 실측 확인) | 31 |
| `HR_근태.csv`(병합본) | 사번 × 년월 | ✅ `사번×년월` 유니크, `사번` 자체는 **비유니크**(1인당 12~15행) | 4,407 |
| `HR_평가.csv` | 사번 × 분기 | ✅ `사번×분기` 유니크, `사번` 자체는 **비유니크**(1인당 4행) | 1,200 |
| `data_hr_referrals` | (미생성) | 해당 없음 — 설계서만 존재, 실제 데이터 없음 | — |

**핵심 구분**: `HR_직원`·`HR_퇴사이력`은 "사번당 1행"(1의 쪽). `HR_근태`·`HR_평가`는 "사번당 여러 행"(다의 쪽). 팬아웃은 항상 **"다의 쪽" 테이블을 조인할 때** 발생한다.

---

## 2. 안전한 조인 (그대로 해도 됨)

### 규칙 A. "1의 쪽" 끼리 조인 — 팬아웃 없음
`HR_직원 (사번 유니크)` ↔ `HR_퇴사이력 (사번 유니크)`
- `사번` 기준 LEFT JOIN. 양쪽 다 사번당 1행이므로 조인 후에도 행 수는 `HR_직원`과 동일(300행)하게 유지됨
- 검증: 조인 후 행수가 조인 전 `HR_직원` 행수(300)와 같은지 확인

### 규칙 B. "1의 쪽" ← "다의 쪽"을 **집계 후** 조인 — 팬아웃 없음
`HR_직원 (사번 유니크)` ← `HR_근태를 사번별로 미리 집계(예: 평균 초과근무시간)`
- 순서: ① `HR_근태.groupby("사번")["초과근무시간"].mean()`으로 **사번당 1행으로 만든 뒤** ② `HR_직원`에 LEFT JOIN
- 이렇게 하면 조인 후에도 300행 유지됨
- **지금 app.py의 `build_base` 함수가 실제로 이 방식을 씀** — `HR_근태`를 사번별 평균으로 만든 뒤 직원 마스터에 붙임 (안전)

---

## 3. 위험한 조인 (팬아웃 발생 — 반드시 피할 것)

### 규칙 C. "다의 쪽" 두 개를 사번만으로 직접 조인 — 절대 금지
`HR_근태(사번당 12~15행)` × `HR_평가(사번당 4행)`를 **사번만으로** 직접 조인하면:
- 한 사번당 15행 × 4행 = **60행**이 생김 (원래 의미 없는 조합, 예: 2025년 1월 근태 행이 2025년 모든 분기 평가와 다 매칭됨)
- **결과**: 이 상태로 평균·합계를 내면 실제보다 몇 배 부풀려진 값이 나옴 — 코드는 에러 없이 돌아가기 때문에 알아채기 가장 어려운 버그

**해결 방법 (택1)**:
1. 두 테이블을 각각 사번별로 집계해서 "1의 쪽"으로 만든 뒤 조인 (예: 근태→사번별 평균, 평가→사번별 평균, 그 다음 조인) — 지금 대시보드가 쓰는 방식
2. 정말 월×분기 교차 분석이 필요하면, **분기-월 매핑 키를 명시적으로 만들어서** (예: 2025-01~03월 → 2025Q1) 그 매핑 키로 조인해야 함 — `사번`만으로는 절대 안 됨

### 규칙 D. 병합 시 그레인이 다른 두 원본을 그대로 concat — 정합성 깨짐
Clean 5-1에서 실제로 겪은 사례: `HR_근태_2026_1분기_원본수령분.xlsx`의 소계 행(부서×월 그레인)과 상세 행(사번×월 그레인)을 구분 없이 그대로 이어 붙이면, 이후 어떤 조인에서든 "부서명이 사번 자리에 들어간 유령 행"이 섞여 팬아웃보다 더 나쁜 **오염**이 생김
- **해결**: 병합·조인 전에 반드시 그레인이 통일된 상태인지 먼저 확인(regex/개수 검증), 다른 그레인 행은 분리 보관

### 규칙 E. 이력성 테이블을 "현재 상태"처럼 조인 — 시점 불일치
`HR_퇴사이력.퇴사일`은 사건 시점 값인데, 이걸 `HR_근태`의 특정 월 행과 조인해서 "그 달에 퇴사한 사람만" 걸러내려면 **`퇴사일`의 연월과 `HR_근태.년월`이 정확히 같은 행만** 남겨야 함 — `사번`만으로 조인하면 그 사람의 재직 중이던 다른 달 근태까지 다 딸려 나옴(이것도 일종의 팬아웃)

---

## 4. 조인 전 체크리스트 (매번 확인)

1. **조인하려는 각 테이블의 키가 유니크한지 먼저 확인한다** — `df["키"].is_unique` 또는 `df.duplicated(["키"]).any()`
2. 두 테이블 다 "다의 쪽"이면 → 먼저 집계해서 "1의 쪽"으로 만들거나, 명시적 매핑 키를 만든다 (규칙 C)
3. 조인 후 **행 수를 예측하고 실제와 비교한다** — 예상과 다르면 팬아웃 발생 신호
4. LEFT JOIN을 기본으로 쓴다 (INNER JOIN은 매칭 안 되는 행이 조용히 사라져서 다른 종류의 착시를 만듦)
5. 조인 후 핵심 지표(합계·인원수 등)를 조인 전과 대조해서 값이 부풀지 않았는지 확인한다 (Clean 5-1의 소계 대조 검증과 같은 원리)

---

## 5. 이 프로젝트에서 실제로 써야 할 조인 순서 (권장)

```
HR_직원(300, 사번 유니크)
  ← LEFT JOIN HR_퇴사이력(31, 사번 유니크)          [규칙 A: 안전]
  ← LEFT JOIN HR_근태.groupby(사번).mean()(300)      [규칙 B: 집계 후 조인, 안전]
  ← LEFT JOIN HR_평가.groupby(사번).mean()(300)      [규칙 B: 집계 후 조인, 안전]
```
→ 최종 결과도 300행 유지되어야 정상. 300행이 아니면 어딘가에서 팬아웃이 발생한 것.

**절대 하면 안 되는 순서**:
```
HR_근태(4407) × HR_평가(1200) 를 사번만으로 직접 조인   ❌ 규칙 C 위반
```

---

## 6. 확인 필요

- `data_hr_referrals`가 나중에 실제로 만들어지면, `referrer_사번`·`referred_사번` 둘 다 `HR_직원.사번`을 참조하는 "사건 1건=1행" 테이블이 될 예정 — 이 경우도 규칙 A(1의 쪽끼리)에 해당하므로 안전하게 조인 가능할 것으로 예상되나, 실제 생성 후 유니크성 재검증 필요
