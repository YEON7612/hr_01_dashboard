# 나의 성장퍼널

**도메인**: HR 데이터(hr_01_dashboard, HR_직원·HR_근태·HR_퇴사이력·HR_평가 4개 테이블, 300명)로 "직원들은 왜 퇴사하는가"를 진단하고, 부서·근속연수·초과근무·평가등급·채용경로 등 요인과 퇴사의 관계를 근거로 개선을 제안한다.

---

## 2026-08-25 — 그레인·기간·결측 확인, 조인 함정

**오늘 한 것**

`project3-report/data/`의 parquet 9개(합계 1,463,518행) 전부에 대해 그레인·기간·결측·기본키 후보를 확인하고 외래키 겹침을 검증했다. customers(50,000명)와 funnel_events를 visitor_id로 INNER JOIN하니 6,702명만 남아, 나머지 43,298명(87%)이 조용히 사라지는 조인 함정을 확인했다.

**고정**

INNER JOIN으로 두 테이블을 합치면 한쪽에만 있는 값은 조용히 빠진다. 조인 전후 모집단 크기(행수 또는 유일 개체 수)를 반드시 비교하고, 줄었다면 그 차이를 설명할 수 있어야 한다. 설명이 안 되면 조인 키나 조인 방식(INNER/LEFT)이 잘못된 것이다.

**가변**

| 수업/기법 개념 | 내 도메인에서 |
|---|---|
| 두 컬럼이 서로 같은 사실을 가리키는지 대조 | `customers.churn_date` ↔ `customers.is_churned` |
| 조인 전후 모집단 크기 비교 | `customers` × `funnel_events` (`visitor_id` 키, 50,000명 → 6,702명) |
| 외래키가 상위 테이블에 다 있는지 겹침 검사 | 9개 테이블 전체(`campaign_id`→campaigns, `visitor_id`→sessions 등) |

**없으면**

| 없는 것 | 어떻게 할 것인가 |
|---|---|
| `customers`의 87%(43,298명)에 `visitor_id`·`campaign_id` 없음(2025년 이전 가입자) | 퍼널·캠페인 관련 분석은 2025년 가입자(6,702명)로 모집단을 한정한다 |
| `sessions`의 38%(111,264건)에 `campaign_id` 없음 | 캠페인 효율 분석은 campaign_id 있는 세션만 쓰고, 채널 전체 트래픽 지표와 구분한다 |
| `support_tickets`의 70%(22,980건)에 `satisfaction_score` 없음 | 만족도 지표는 표본 30%(9,744건) 기준이며, 추가 세그먼트로 쪼갤 때마다 최소표본 30을 재확인한다 |

---

## 2026-08-26 — 가입 퍼널·visitor_id 전환 개념을 HR 퇴사 도메인으로 번역

**오늘 한 것**

`hr_01_dashboard`(HR_직원·HR_근태·HR_퇴사이력·HR_평가 4개 CSV, 300명 규모)의 컬럼 구조를 확인하고, 7주차 Day1의 가입 퍼널(랜딩방문→요금제조회→신청시작→신청완료→개통→첫결제, 6단계)·`visitor_id`→`customer_id` 전환 개념을 이 도메인에 대입했다. 채용 단계별 이벤트 로그(지원→서류→면접→오퍼→입사)가 데이터에 없어 가입 퍼널과 정확히 대응하는 데이터는 없다는 것을 확인했고, 대신 입사일·퇴사일로 계산되는 근속 생존 퍼널(이미 근속구간별 퇴사율로 실측 완료 — 1년 미만 42.1%, `04_insights/근속구간별_퇴사율_실측결과.md`)을 대안으로 정했다. 사번은 입사 시점부터 유일 식별자로 부여되어 있어, 익명→식별 전환(`visitor_id`→`customer_id`) 같은 별도 전환 이벤트 자체가 이 도메인엔 없다는 것도 확인했다.

**고정**

여러 단계를 거치는 전환 과정을 분석하려면 각 단계가 개별 이벤트 행으로 로그에 남아 있어야 한다 — 최종 결과 하나만 담은 컬럼(성공/실패, 있음/없음)은 퍼널이 아니라 상태값이다. 그리고 식별자가 여정의 출발점부터 고정되어 있으면, 익명→식별 전환이라는 문제 자체가 처음부터 존재하지 않는다.

**가변**

| 수업에서 | 내 도메인에서 |
|---|---|
| 고객(customer) | 직원(사번 단위, `HR_직원.사번`) |
| 이탈(churn) | 퇴사(`HR_퇴사이력`에 사건 발생, `HR_직원.재직상태='퇴사'`) |
| `visitor_id` → `customer_id` 전환(익명→식별) | 해당 없음 — `사번`이 입사일부터 유일 식별자로 부여되어 처음부터 식별됨. 전환 이벤트 자체가 없으므로 그대로 둔다 |
| 가입 퍼널(랜딩방문→요금제조회→신청시작→신청완료→개통→첫결제, 6단계 이벤트 로그) | 정확히 대응하는 데이터 없음(아래 '없으면' 참고). 가장 가까운 대안은 **근속 생존 퍼널**: 입사 → 1년 생존 → 3년 생존 → 5년 생존 → 10년 생존(각 단계 생존율로 이탈 지점 확인, `HR_직원.입사일` + `HR_퇴사이력.퇴사일`로 계산) |

**없으면**

| 없는 것 | 어떻게 할 것인가 |
|---|---|
| 가입 퍼널과 같은 다단계 이벤트 로그(채용의 경우: 지원→서류합격→면접→오퍼→입사) — `funnel_events`처럼 단계별 타임스탬프가 찍힌 테이블 자체가 없음, `HR_직원.채용경로`는 결과 카테고리(공채/수시/추천/헤드헌팅) 하나뿐 | 다단계 채용 퍼널 분석은 포기하고, 데이터가 실제로 지원하는 근속 생존 퍼널(입사→1/3/5/10년 생존율)로 대체한다. 이미 근속구간별 이탈 인원까지 실측되어 있어(1년 미만 8명·1~3년 14명·3~5년 3명·5~10년 6명·10년 이상 0명, 퇴사자 31명 기준) 계산만 생존율 형태로 다시 정리하면 된다 |
| 익명 방문자 식별 이벤트(`visitor_id`가 언제 `customer_id`로 바뀌는지) | 사내 시스템이라 처음부터 사번으로 식별됨. 전환 단계 자체가 없으므로 그대로 둔다 — 다만 이 특성 때문에 "가입 전 이탈"(랜딩~신청완료 단계에서의 이탈)은 이 도메인엔 정의 자체가 불가능하다는 점을 명시해야 함 |
| '첫결제'처럼 명확한 단일 전환·수익 이벤트 | 실제 급여·매출 데이터가 없어 인건비 효율 지표는 공개통계 추정치로 시도했다가 폐기됨(`README.md`). Revenue 단계는 분기별 `평가등급`(성과)으로 대체해 쓰고 있음 |

---

## 2026-08-26 (project3-wiki 세션 — 텔레콤 퍼널 기기·채널 병목) — 세그먼트 분해 기법을 HR로 번역

> 위 "가입 퍼널·visitor_id 전환" 항목과 같은 날짜지만 다른 세션·다른 작업이다. 저 항목은 **근속 생존 퍼널로 대체하기로 정한 결정**을 다뤘고, 이 항목은 project3-wiki(통신사 가입 퍼널 실습) 세션에서 **오늘 실제로 계산한 "구간을 세그먼트로 쪼개 병목을 찾는 기법"**을 HR 쪽에 번역한 것이다.

**오늘 한 것**

project3-wiki 세션에서 가입 퍼널 6단계를 사람(visitor_id) 기준으로 계산하고(요금제조회→신청시작 구간이 전환율 23.60%로 가장 낮음), 신청시작→신청완료 구간을 기기별(mobile 35.89% vs desktop 55.24%·tablet 56.66%)·채널별(추천 41.40%~제휴 43.14%, 상단 구간은 채널 간 최대 21%p 차이)로 쪼개 병목이 상단 구간과 mobile 기기에 집중된 것을 확인했다.

**고정**

전체 구간 하나의 평균 전환율(또는 생존율)만 보면 병목이 어디 있는지 알 수 없다. ① 여정을 구간으로 나누고 ② 각 구간을 카테고리형 변수(세그먼트)로 쪼개야, "어느 구간이 문제인가"와 "그 구간에서 어느 세그먼트가 문제인가"를 동시에 좁힐 수 있다.

**가변**

| 오늘(project3, 텔레콤)에서 | 내 도메인(HR)에서 |
|---|---|
| 신청시작→신청완료 하나의 구간 | 근속 생존 퍼널의 한 구간(예: 입사→1년 생존) — 위 항목에서 이미 정의됨 |
| 기기(mobile/desktop/tablet)로 쪼개 병목 위치 찾기 | 부서(`HR_직원.부서`)·직급(`HR_직원.직급`)·초과근무 구간(`HR_근태.초과근무시간`)으로 쪼개 병목 위치 찾기 — 후보만 정함, 실측은 아직 안 함 |
| 채널(검색/디스플레이/제휴/추천/오프라인)로 쪼개 병목 위치 찾기 | 채용경로(`HR_직원.채용경로`)로 쪼개 병목 위치 찾기 — 개념은 대응되나 실측은 아직 안 함 |
| 상단(방문→조회) / 하단(신청시작→완료)처럼 구간을 나눠 따로 보기 | 근속 생존 퍼널의 어느 구간(0~1년 / 1~3년 / 3~5년 등)에서 이탈이 몰리는지 나눠 보는 것과 대응 |

**없으면**

| 없는 것 | 어떻게 할 것인가 |
|---|---|
| 오늘 텔레콤 쪽에서 한 "기기·채널별 병목 실측"에 정확히 대응하는 HR 쪽 계산값 | 아직 계산하지 않았다. 이번 항목은 기법·개념 매핑까지만 하고, 부서별·채용경로별·초과근무 구간별로 실제 1년 이내 이탈률을 쪼개 병목을 찾는 계산은 다음 회차(또는 요청 시)로 미룬다 — 위 근속 생존 퍼널 실측(1년 미만 42.1%)은 전체 평균값이라 세그먼트별 분해가 아니다 |

---

## 2026-08-26 — "근속 1년 미만 퇴사율 42.1%" 분모·분자 재확인, HR_직원↔HR_퇴사이력 대조

**오늘 한 것**

`hr_01_dashboard`의 `HR_직원.csv`(재직상태='퇴사')와 `HR_퇴사이력.csv`(사번 유일값)를 원본 데이터로 직접 대조했다 — 양쪽 다 31건으로 정확히 일치하고, 어긋난 사번은 0건이었다(두 집합이 완전히 동일). 이어서 `04_insights/근속구간별_퇴사율_실측결과.md` 등 여러 문서(01_questions, 03_analysis, report, INDEX.md)에 반복 인용되는 "근속 1년 미만 퇴사율 42.1%"가 정확히 어떤 분모·분자로 나온 값인지 원본 데이터로 재계산해 확인했다.

**고정**

한 지표를 여러 방식(전사 대비/이탈자 대비/코호트 대비)으로 정의할 수 있을 때, 인용할 때마다 분모·분자를 명시하지 않으면 숫자만 보고는 어느 정의인지 알 수 없다. 또한 경계값(예: 근속 정확히 1.0년) 근처에서는 반올림 적용 여부만으로 구간에 속하는 인원 자체가 바뀔 수 있다 — 구간 판정은 원본 정밀도로 하고, 반올림은 표시 단계에서만 적용해야 한다.

**검증 결과**

| 구분 | 분모 | 분자 | 비율 |
|---|---|---|---|
| (a) 전사 대비 | 전체 300명 | 1년 미만 재직 후 퇴사 8명 | 2.7% |
| (b) 퇴사자 대비 | 퇴사자 31명 | 그 중 1년 미만이었던 8명 | 25.8% |
| **(c) 실제 "42.1%"의 정의 = 코호트(구간) 대비** | 근속 1년 미만 구간 전체 인원 19명(재직·퇴사 통틀어) | 그 중 퇴사한 8명 | **42.1%** ✅ 원본 재계산과 정확히 일치 |

기존 문서들은 "42.1%"만 인용하고 (a)/(b)/(c) 중 어느 정의인지는 명시하지 않았다 — 실제로는 (c), 즉 "그 근속 구간을 거쳐간 사람들(현재 재직 중이든 이미 퇴사했든) 중 퇴사로 이어진 비율"이다. (a)·(b)와 수치 차이가 크므로(2.7%/25.8%/42.1%), 앞으로 이 수치를 인용할 때는 "근속 1년 미만 구간 코호트 19명 기준"이라고 분모를 함께 적어야 한다.

⚠️ 반올림에 따라 구간 경계 인원이 달라짐: 근속연수를 소수 1자리로 반올림(`calc_metrics.py`의 `tenure_years` 지표와 동일 방식)한 뒤 <1년으로 거르면 17명(퇴사 6명, 35.3%)이 되어 원 문서의 42.1%와 달라진다. 42.1%는 반올림 없이(raw 근속연수) 구간을 나눴을 때만 정확히 재현됨 — 데이터가 바뀐 게 아니라 반올림 적용 시점의 차이였다.

---

## 2026-08-26 — 코호트 우측절단(censoring) 문제 확인, 신규 입사자 1년 내 이탈률 정정(42.1% → 2.8%)

**오늘 한 것**

위 항목의 "근속 1년 미만 구간 코호트 19명" 중 재직 중인 11명이 실제로는 아직 근속 1년을 채우지 못해 이탈 여부가 확정되지 않은 사람(우측절단, right-censored)이라는 것을 확인했다. 이들을 단순히 분모에서만 빼면(19명→8명) 남는 사람이 이미 퇴사한 8명뿐이라 8/8=100%라는 항등식만 나와 무의미했다. 대신 입사일 기준으로 "결과가 이미 확정된 사람"만 모집단으로 다시 잡았다: (1) 입사일이 2024-12-31 이전인 사람 전체(이미 1년 마크를 지나 생존/이탈 여부가 확정됨) + (2) 입사일과 무관하게 입사 후 365일 이내 퇴사한 사람(결과가 이미 나옴). 입사일 2025년 이후이면서 아직 재직 중인 11명만 제외한 결과, 포함 대상 289명 중 입사 365일 이내 퇴사자는 8명 → **2.8%**.

**고정**

우측절단된 관측치(아직 결과가 안 나온 대상)를 분자·분모 처리 없이 방치하거나, 분모에서만 제거하고 분자는 그대로 두면 비율이 왜곡된다 — 절단된 관측치는 결과가 나올 때까지 분자·분모 양쪽에서 함께 제외하거나 생존분석(Kaplan-Meier 등) 기법으로 별도 처리해야 한다. 스냅샷 시점의 "현재 근속연수" 구간으로 코호트를 나누면, 최근 입사자 중 아직 이탈하지 않은 사람들이 분모에 섞여 절단 편향이 생긴다.

**세 가지 정의 비교**

| 정의 | 분모 | 분자 | 결과 |
|---|---|---|---|
| 스냅샷 코호트(기존 인용값) | 근속 1년 미만 구간 19명(재직+퇴사 통틀어) | 8명 | 42.1% |
| 재직자(절단 관측치) 단순 제외 | 19명 중 재직자 11명 제외 → 8명 | 8명 | 100%(항등식, 무의미) |
| **입사일 기준(결과 확정자만)** | 결과 확정된 289명(장기근속 생존자 288명 포함) | 8명 | **2.8%** |

**⚠️ 정정**

기존에 여러 문서(`04_insights/근속구간별_퇴사율_실측결과.md`, `03_analysis`, `01_questions`, `report/퇴사원인_진단_리포트.md`, `INDEX.md`)에 인용된 **42.1%는 스냅샷 코호트의 절단(우측절단) 편향으로 부풀려진 값**이었다. 실제 신규 입사자 1년 내 이탈률은 **2.8%**다. 이후 "온보딩 문제"를 이탈 병목으로 가정하는 모든 분석(위 "근속 생존 퍼널" 대안 포함)은 이 정정된 수치를 기준으로 재검토가 필요하다.

---

## 2026-08-26 — 입사 1년 내 이탈률 세그먼트 분해(부서·채용경로·초과근무), 병목 후보 판정

**오늘 한 것**

정정된 입사일 기준 코호트(289명, 이탈 8명, 2.8%)를 부서별·채용경로별·초과근무 3분위(사번별 평균 초과근무시간 기준)로 나눠 세그먼트별 입사 1년 내 이탈률을 계산했다. 가장 높은 세그먼트를 어제 배운 병목 판단 기준(①개선 여지 ②원인 특정 가능 ③통제 가능)에 대입해 평가했다.

**부서별**

| 부서 | 코호트n | 이탈수 | 이탈률 |
|---|---|---|---|
| **개발** | 51 | 3 | **5.9%** |
| CS | 48 | 2 | 4.2% |
| 영업 | 51 | 2 | 3.9% |
| 재무 | 61 | 1 | 1.6% |
| 생산 | 37 | 0 | 0.0% |
| 인사 | 41 | 0 | 0.0% |

**채용경로별**

| 채용경로 | 코호트n | 이탈수 | 이탈률 |
|---|---|---|---|
| 공채 | 208 | 7 | 3.4% |
| 수시 | 44 | 1 | 2.3% |
| 추천 | 26 | 0 | 0.0% |⚠️ n<30 |
| 헤드헌팅 | 11 | 0 | 0.0% |⚠️ n<30 |

**초과근무 3분위**(하위 <10.3h, 중위 10.3~16.6h, 상위 >16.6h)

| 구간 | 코호트n | 이탈수 | 이탈률 |
|---|---|---|---|
| **상위(1/3)** | 96 | 4 | **4.2%** |
| 중위(1/3) | 96 | 3 | 3.1% |
| 하위(1/3) | 97 | 1 | 1.0% |

**병목 후보 3기준 평가**(⚠️ 아래 둘 다 "확정된 병목"이 아니라 **병목 후보**로만 표시)

| 기준 | 개발 부서(5.9%) | 초과근무 상위 구간(4.2%) |
|---|---|---|
| ① 개선 여지 | 전사(2.8%)의 약 2배 — 수치상으론 있음 | 하위 구간(1.0%) 대비 4배, 단조 증가 패턴 |
| ② 원인 특정 가능 | ❌ 아직 못 함 — 부서명뿐, 왜 개발부서인지 미조사 | △ "과로→이탈"은 그럴듯한 인과 스토리지만 상관관계일 뿐 확정 아님 |
| ③ 통제 가능 | △ 부서 자체는 통제 대상이 아니고, 그 안의 구체적 요인을 알아야 개입 가능 | ✅ 초과근무는 정책(인력배치·업무량 조정)으로 직접 통제 가능 |

**고정**

세그먼트 분모(n)가 최소표본 기준(30)을 넘겨도, 사건 자체가 희귀하면(여기선 이탈 0~7건) 분자가 너무 작아 세그먼트 간 차이가 실제 신호인지 우연인지 통계적으로 구분하기 어렵다 — 표본 경고는 분모 크기만이 아니라 사건(분자) 수도 함께 봐야 한다.

**⚠️ 표본 경고**: 전 세그먼트 분모 n≥30(추천·헤드헌팅 제외)이지만, 분자(이탈 건수)는 0~7건뿐이라 개발 부서 5.9%는 단 3명, 초과근무 상위 4.2%는 단 4명이 좌우한 값이다. 이 정도 사건 수로는 어느 쪽도 아직 "확정된 병목"이라 부를 근거가 부족하다 — ②(원인 특정)를 특히 만족하지 못한다.

**할 일**

- [ ] 입사 1년 내 이탈 8명의 실제 퇴사사유(`HR_퇴사이력.csv`의 `퇴사사유`)를 확인해 개발 부서·고초과근무와 실제로 연결되는지 원인을 좁힐 것 — 지금까지는 부서·초과근무 구간 단독 breakdown만 했고, 8명 각각의 퇴사사유는 아직 들여다보지 않았음.

---

## 2026-08-27 (project3-wiki 세션 — 관찰→가설→가드레일→반증조건 형식화) — 검증 가능한 가설을 만드는 형식을 HR로 번역

**오늘 한 것**

project3-wiki 세션에서 텔레콤 관찰 3건(모바일 신청완료율 35.89% vs 데스크톱 55.24%, 19.35%p 차이 / 제휴 채널 요금제조회 도달율 35.56% vs 추천 56.82% / 조회→신청시작 전환율 23.60%, 6단계 중 최저)을 조작적 정의를 가진 가설 3개로 바꾸고, 각 가설에 가드레일(예: 90일 유지율, 3%p 사업 기준)과 반증 조건 3가지(효과크기 미달·통계적 유의성 p≥0.05·가드레일 위반)를 붙이는 형식을 실습했다.

**고정**

관찰(사실)과 해석(추정)은 분리해야 한다. 해석에서 나온 가설은 조작적 정의로 적어야 검증 가능해진다. 가설에는 주지표를 올리려다 희생될 수 있는 것(가드레일)과, 가설이 틀렸다고 인정할 기준(반증 조건: 효과크기 미달·통계적 유의성 미달·가드레일 위반)을 함께 붙여야 실험으로 옮길 수 있다.

**가변**

| 오늘(텔레콤)에서 | 내 도메인(HR)에서 |
|---|---|
| 관찰(mobile 35.89% vs desktop 55.24%, 19.35%p; 제휴 35.56% vs 추천 56.82%; 조회→신청시작 23.60%, 6단계 중 최저) | 관찰(2026-08-26 실측): 초과근무 상위 1/3 구간 입사 1년 내 이탈률 4.2%(96명 중 4명) vs 하위 1/3 1.0% |
| 조작적 정의(본인인증 3단계→2단계 / 랜딩 페이지 통일 / 클릭 2단계→1단계) | 초과근무 상위 1/3 구간(월평균 16.6h 초과) 인원의 초과근무를 중위 구간(10.3~16.6h) 수준으로 줄인다 |
| 주지표 + 현재값(application_completion_rate 등, 35.89%/35.56%/23.60%) | 입사 1년 내 이탈률(초과근무 상위 구간), 현재값 4.2% |
| 가드레일(90일 유지율, 3%p, 사업 기준) | 업무 성과·평가등급(`HR_평가`로 확인 가능) — 기준값·근거는 다음 회차에서 정한다 |
| 목표 효과크기 근거(기존 실험 5건 실측 lift 참고) | 없음 — HR엔 과거 개입 실험 이력이 없다(아래 "없으면" 참고) |
| 반증 조건(효과크기 미달 / p≥0.05 / 가드레일 위반) | 효과크기 미달·가드레일 위반은 그대로 적용 가능. 통계적 유의성(p≥0.05)은 표본(이탈 4명)이 너무 작아 그대로 쓰기 어렵다(아래 "없으면" 참고) |

**없으면**

| 없는 것 | 어떻게 할 것인가 |
|---|---|
| A/B 실험 데이터(목표 효과크기 근거로 쓸 과거 유사 개입의 실측 lift) | HR엔 이런 실험 이력이 없다. 전후 비교·층화 분석으로 대체하고, **인과 주장은 하지 않는다**는 단서를 반드시 붙인다 |
| 충분한 표본(오늘 쓴 p≥0.05 이표본 비율검정은 표본 수천 단위를 가정) | 초과근무 상위 구간 이탈은 4명뿐이다(위 "표본 경고" 문단과 같은 문제) — p값 대신 **기술 통계**(격차의 크기·방향만 봄)로 반증 조건을 다시 쓴다. 억지로 검정하지 않는다 |
| 가드레일 숫자를 사업적으로 정할 담당자 | 텔레콤 쪽은 "사업적 기준"이라고 확인해줄 사람이 있었다. HR 쪽은 업무 성과·평가등급이 얼마나 나빠지면 중단할지 누가 정할지 아직 안 정해졌다 — 다음 회차 과제로 남긴다 |

---
