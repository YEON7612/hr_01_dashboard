# 지표정의서 — HR 퇴사 원인 진단 대시보드

작성일: 2026-08-01
근거: `나의_분석_워크플로우.md` 6단계(Model), `app.py` 실제 계산 로직
원칙: 10단계 워크플로우 6단계("지표 정의 확정 — 변별력 점검") 그대로 적용

각 지표는 아래 9개 항목으로 정의합니다: ①지표명 ②정의(계산식) ③그레인 ④사용 데이터/컬럼 ⑤포함·제외 기준 ⑥변별력 점검 ⑦표본 크기 주의 ⑧계산 예시 ⑨상태/비고

---

## 1. 전사 퇴사율

| 항목 | 내용 |
|---|---|
| ② 정의 | 퇴사인원 ÷ 전체인원 × 100 |
| ③ 그레인 | 한 값 = 전사 전체 (기간 구분 없음, 누적 기준) |
| ④ 사용 데이터 | `HR_직원.재직상태` |
| ⑤ 포함·제외 | 재직·퇴사 상태만 집계(그 외 상태값 없음). 스코프 필터(전사/부서 선택) 적용 시 선택된 범위로 분모·분자 재계산 |
| ⑥ 변별력 점검 | 부서·채용경로별로 값이 갈리는지 확인됨(11~19%대로 분산) → 통과 |
| ⑦ 표본 크기 | 전사 300명 기준이라 표본 문제 없음 |
| ⑧ 계산 예시 | 코드상 `overall_rate = round(total_leave / total_emp * 100, 1)` |
| ⑨ 상태 | 확정, 대시보드 KPI 카드에 반영됨 |

## 2. 부서별 퇴사율

| 항목 | 내용 |
|---|---|
| ② 정의 | 부서 내 퇴사인원 ÷ 부서 내 전체인원 × 100 |
| ③ 그레인 | 한 값 = 한 부서 |
| ④ 사용 데이터 | `HR_직원.부서`, `HR_직원.재직상태` |
| ⑤ 포함·제외 | 6개 부서(CS/개발/생산/영업/인사/재무) 전부 포함 |
| ⑥ 변별력 점검 | 최고~최저 부서 간 격차 존재(개발 19.2% 등) → 통과 |
| ⑦ 표본 크기 | 부서당 34~64명 — 문제 없는 수준 |
| ⑧ 계산 예시 | "퇴사율 최고 부서" KPI 카드 = `dept_leave_rates.idxmax()` |
| ⑨ 상태 | 확정 |

## 3. 근속기간(년)

| 항목 | 내용 |
|---|---|
| ② 정의 | (기준일 − 입사일) ÷ 365.25, 소수 1자리 반올림. 재직자는 기준일=오늘, 퇴사자는 기준일=퇴사일 |
| ③ 그레인 | 한 값 = 한 사번 |
| ④ 사용 데이터 | `HR_직원.입사일`, `HR_퇴사이력.퇴사일` |
| ⑤ 포함·제외 | 전 직원 포함 (재직·퇴사 무관), 366.25일 아니라 365.25일 사용(윤년 보정) |
| ⑥ 변별력 점검 | 근속기간 x 평가점수 산점도에서 분산 확인됨 → 통과 |
| ⑦ 표본 크기 | 300명 전체 |
| ⑧ 계산 예시 | 코드: `((기준일 − 입사일).dt.days / 365.25).round(1)` |
| ⑨ 상태 | 확정 |

## 4. 평가점수 (등급→점수 환산)

| 항목 | 내용 |
|---|---|
| ② 정의 | S=5, A=4, B=3, C=2, D=1로 환산 후 사번별 평균 |
| ③ 그레인 | 한 값 = 한 사번 (여러 분기 평가를 평균) |
| ④ 사용 데이터 | `HR_평가.평가등급` |
| ⑤ 포함·제외 | 2025년 4개 분기만 존재 — **2025년 이전 평가 이력 없음**(커버리지 한계, Profile 단계에서 이미 확인됨) |
| ⑥ 변별력 점검 | 부서별 평균이 갈리는지 확인됨(1~5점 분산 존재) → 통과 |
| ⑦ 표본 크기 | 300명 × 최대 4분기 |
| ⑧ 계산 예시 | 코드: `grade_score_map = {"S":5,"A":4,"B":3,"C":2,"D":1}` |
| ⑨ 상태 | 확정. 단, "2025년 한정" 커버리지 한계를 보고서에 항상 병기할 것 |

## 5. 채용경로별 퇴사율·근속연수·평가점수

| 항목 | 내용 |
|---|---|
| ② 정의 | 채용경로(공채/수시/추천/헤드헌팅)별로 퇴사율·평균근속연수·평균평가점수를 그룹 집계 |
| ③ 그레인 | 한 값 = 한 채용경로 |
| ④ 사용 데이터 | `HR_직원.채용경로`, `HR_직원.재직상태`, 근속기간(3번), 평가점수(4번) |
| ⑤ 포함·제외 | 화이트리스트 4종만 허용(공채/수시/추천/헤드헌팅) — 그 외 값은 데이터 오류로 간주 |
| ⑥ 변별력 점검 | 추천채용 퇴사율 0%로 나머지와 뚜렷이 구분됨 → 통과 (단, ⑦ 참고) |
| ⑦ 표본 크기 | ⚠️ **추천채용 27명, 헤드헌팅 11명 — 표본이 작아 "신호" 수준으로만 해석**. 대시보드·리포트에 반드시 표본 수를 함께 표기 |
| ⑧ 계산 예시 | 코드: `ch_base.groupby("채용경로")["평가점수"].mean().reindex(CHANNEL_ORDER)` |
| ⑨ 상태 | 확정. Referral 세부지표(추천인당 성공 건수 등)는 `설계서_data_hr_referrals.md`에 의해 **영구 불가 판정**(실제 인사팀 연결기록 없음) |

## 6. 부서별 초과근무시간 (평균)

| 항목 | 내용 |
|---|---|
| ② 정의 | 부서 내 사번별 초과근무시간(월별)의 평균 |
| ③ 그레인 | 한 값 = 한 부서 (기간 구분 없이 보유 기간 전체 평균) |
| ④ 사용 데이터 | `HR_근태.초과근무시간` (2025-01~2026-03, 병합본 반영 후) |
| ⑤ 포함·제외 | `기본근무시간_가정값` 컬럼은 지표 계산에서 제외(Clean 5-1 규칙4) |
| ⑥ 변별력 점검 | 부서 간 격차 존재(개발 부서 최고 등) → 통과 |
| ⑦ 표본 크기 | ⚠️ 2026년 1분기 데이터는 합성 생성분이며, **개인 단위 연속성이 낮음**(Validate 8-1: 상관계수 0.279) — 부서 단위 집계만 신뢰, 개인별 추이 분석에는 사용 금지 |
| ⑧ 계산 예시 | 코드: `base.groupby("부서")["초과근무시간"].mean().round(2)` |
| ⑨ 상태 | 확정(부서 단위 한정) |

## 7. 목표 잔류율 달성률

| 항목 | 내용 |
|---|---|
| ② 정의 | 잔류율(=100−전사퇴사율) ÷ 목표잔류율(90%) — 도넛 게이지로 시각화 |
| ③ 그레인 | 한 값 = 전사 전체 |
| ④ 사용 데이터 | 1번(전사 퇴사율)에서 파생 |
| ⑤ 포함·제외 | 목표값(90%)은 하드코딩된 상수(`TARGET_RETENTION_RATE`) — 실제 인사팀 목표치로 교체 필요 여부 확인 안 됨 |
| ⑥ 변별력 점검 | 해당 없음(목표 대비 진행률 지표라 변별력보다는 목표달성 여부가 핵심) |
| ⑦ 표본 크기 | 전사 300명, 문제 없음 |
| ⑧ 계산 예시 | 코드: `retention_rate = round(100 - overall_rate, 1)` |
| ⑨ 상태 | **확인 필요** — 90%가 실제 인사팀이 정한 목표치인지, 임의로 설정한 값인지 출처 미확인. 확인 전까지 대시보드 게이지 옆에 "출처 미확인 가정값" 문구를 노출하도록 `app.py`에 caption 추가함(2026-08-22). |
| 엔진 편입 여부 | `calc_metrics.py`가 지원하는 4개 `formula_type`(ratio/mean/pct_threshold/pct_change) 중 어느 것도 "100 − 다른 지표" 형태의 파생 계산에 맞지 않아, 엔진에 새 formula_type을 추가하는 대신 **의도적으로 `app.py`에서 직접 계산**하기로 결정함. 엔진 밖에서 관리되는 유일한 지표이므로, 나중에 지표 엔진 기반 자동 리포트를 만들 때는 이 지표만 별도로 챙겨야 함. |

## 8. 최다 퇴사사유

| 항목 | 내용 |
|---|---|
| ② 정의 | 전체 퇴사자의 `퇴사사유` 최빈값 |
| ③ 그레인 | 한 값 = 전사 전체 |
| ④ 사용 데이터 | `HR_퇴사이력.퇴사사유` (화이트리스트: 개인사유/건강/계약만료/이직/이직(경쟁사)) |
| ⑤ 포함·제외 | 퇴사자가 0명이면 "퇴사자 없음"으로 표시(예외 처리됨) |
| ⑥ 변별력 점검 | 5개 사유 간 분포 차이 존재 → 통과 |
| ⑦ 표본 크기 | 전체 퇴사자 31명 기준 — 사유별로 쪼개면 표본이 더 작아지므로 세부 해석 시 주의 |
| ⑧ 계산 예시 | 코드: `leavers_all["퇴사사유"].value_counts().idxmax()` |
| ⑨ 상태 | 확정. "건강"일 경우 강조 표시(빨간 하이라이트 카드)로 시각적 우선순위 부여됨 |

## 9. 인건비 효율 (추정) — 폐기됨

| 항목 | 내용 |
|---|---|
| ② 정의 | (과거) 부서별 추정 인건비 ÷ 평균 성과점수 |
| ⑨ 상태 | **폐기 (2026-08-01)**. 잡코리아 직급별 평균연봉 공개 통계 기반 추정치였고 실제 급여 데이터가 아니었음. 사용자 요청으로 대시보드 탭 자체를 삭제함. **재작성 시 참고**: 실제 급여 데이터가 확보되면 이 정의를 그대로 재사용하되, 반드시 "실제 데이터"로 교체한 값으로 재계산할 것 — 공개 통계 대체는 다시 쓰지 않는다 |

## 10. 연차별 잔류율 (통계적으로는 Kaplan-Meier 생존추정) + 입사 1년 내 이탈률 세그먼트

| 항목 | 내용 |
|---|---|
| ② 정의 | 채용 다단계 이벤트 로그가 없어 가입 퍼널 대신 도입한 대체 지표. 입사 → 1/3/5/10년차 잔류율을 Kaplan-Meier 생존추정으로 계산(재직자는 우측절단 관측치로 처리 — 아직 결과가 안 나온 인원도 놓치지 않고 반영). 보조 지표로 "입사 1년 내 이탈률"을 부서·채용경로·초과근무 3분위로 세그먼트 분해 |
| ③ 그레인 | 생존 퍼널은 한 값 = 전사(또는 사이드바 선택 부서) 전체. 세그먼트 분해는 한 값 = 한 세그먼트(부서/채용경로/초과근무구간) |
| ④ 사용 데이터 | `HR_직원.입사일·재직상태`, `HR_퇴사이력.퇴사일`, `HR_근태.초과근무시간`(사번별 평균) |
| ⑤ 포함·제외 | 세그먼트 분해는 "결과 확정" 코호트만 사용(입사 후 365일 이상 관측됐거나 365일 내 퇴사 확정) — 아직 1년이 안 된 재직자(우측절단)는 제외해 스냅샷 코호트의 편향(성장퍼널 로그 2026-08-26: 42.1%→2.8% 정정 사례)을 반복하지 않음 |
| ⑥ 변별력 점검 | 초과근무 상위 1/3(4.0%) vs 하위 1/3(1.0%), 부서 간 0.0%~5.8% 분산 확인 → 통과 |
| ⑦ 표본 크기 | 최소표본 n≥30(`MIN_SAMPLE`). 채용경로 추천(27)·헤드헌팅(11)은 개별로는 미달 — "추천+헤드헌팅"으로 병합해 n=38로 통과시키되, 이탈 0건이라 95% 신뢰구간 상한이 rule of three로 ~8%까지 열려 있어 "0%=안전"으로 읽지 않음. 사건(이탈) 수 자체도 세그먼트당 0~7건뿐이라 통계적 유의성은 주장하지 않음(기술통계로만 방향·크기 제시) |
| ⑧ 계산 예시 | 코드: `app.py` tab4의 `kaplan_meier()`, `segment_table()` 함수 |
| ⑨ 상태 | 확정(2026-08-29 Day5, 다음 회차에서 임계값 확정). 주지표 경고선 6%(6명)·위험선 8%(8명), 가드레일(초과근무 상위 구간 평균 평가점수) 기준 2.7점 미만 시 중단. 근거: 현재 상위 구간 평균 평가점수 3.03점(표준편차 0.66)에서 약 0.5 표준편차 하락 지점. 다음 회차에 퇴사사유 원인 규명도 완료 — 초과근무↔이탈 상관은 유지하되 인과 근거는 당초보다 약함(1년내 이탈 8명 중 3명이 초과근무와 무관한 "계약만료", 개발부서 이탈자의 초과근무구간도 비일관, 재직자 평가점수는 초과근무구간별로 유의한 차이 없음 — Welch's t-test p=0.44/0.70) |
| 엔진 편입 여부 | Kaplan-Meier 생존추정과 우측절단 코호트 필터링은 `calc_metrics.py`가 지원하는 5개 `formula_type`(ratio/mean/pct_threshold/pct_change/fifo_gap) 중 어느 것에도 맞지 않아, 7번(목표 잔류율 달성률)과 동일한 이유로 **엔진에 새 formula_type을 추가하는 대신 `app.py`에서 직접 계산**하기로 결정함. 나중에 지표 엔진 기반 자동 리포트를 만들 때는 7번과 함께 이 지표도 별도로 챙겨야 함 |

---

## 이 문서에서 "확인 필요"로 남은 것

| 지표 | 확인 필요 내용 |
|---|---|
| 7. 목표 잔류율 달성률 | 목표값 90%의 출처(인사팀 실제 목표치인지) 미확인. 확인 전까지 대시보드에 "출처 미확인 가정값" 캡션 노출 중(2026-08-22 반영). |
| 6. 부서별 초과근무시간 | 2026년 1분기분은 개인 단위로 쓰면 안 됨(부서 단위 한정 사용) |
| 4. 평가점수 | 2025년 이전 평가 이력 없음 — 장기 추이 분석 시 이 한계를 반드시 명시 |
| 10. 연차별 잔류율 | 임계값·가드레일 확정 완료. 채용경로 표본 부족은 추천+헤드헌팅 병합(n=38)으로 처리했으나 이탈 0건 구간의 신뢰구간은 여전히 넓음(rule of three, 상한 ~8%). 초과근무↔이탈 인과관계는 로지스틱 회귀(부서·채용경로 통제)로도 유의하지 않아(p=0.45, 완전분리 문제) 판단 보류·모니터링 지속으로 결정 |
